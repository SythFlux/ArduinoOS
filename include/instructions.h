#ifndef INSTRUCTIONS_H
#define INSTRUCTIONS_H

// Read and run a single instruction for processTable[index], advancing its PC
// and operating on its private stack.
void execute(int index);

#endif // INSTRUCTIONS_H
